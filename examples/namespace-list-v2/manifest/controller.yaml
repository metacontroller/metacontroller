apiVersion: metacontroller.k8s.io/v1alpha1
kind: CompositeController
metadata:
  name: namespace-list-controller
spec:
  generateSelector: true
  parentResource:
    apiVersion: examples.metacontroller.io/v1alpha1
    resource: namespacelists
  childResources:
  - apiVersion: v1
    resource: configmaps
    updateStrategy:
      method: InPlace
  hooks:
    sync:
      version: v2
      webhook:
        url: http://namespace-list-controller.metacontroller/sync
    customize:
      version: v2
      webhook:
        url: http://namespace-list-controller.metacontroller/customize
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: namespace-list-controller
  namespace: metacontroller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: namespace-list-controller
  template:
    metadata:
      labels:
        app: namespace-list-controller
    spec:
      containers:
      - name: controller
        image: python:3.11
        command: ["python", "/hooks/sync.py"]
        volumeMounts:
        - name: hooks
          mountPath: /hooks
      volumes:
      - name: hooks
        configMap:
          name: namespace-list-controller
---
apiVersion: v1
kind: Service
metadata:
  name: namespace-list-controller
  namespace: metacontroller
spec:
  selector:
    app: namespace-list-controller
  ports:
  - port: 80
